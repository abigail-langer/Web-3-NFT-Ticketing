# 📦 前端依赖安装说明

## 需要安装的新依赖

为了实现检票功能，需要安装以下依赖：

### 1. 二维码生成库
```bash
npm install qrcode
npm install --save-dev @types/qrcode
```

### 2. 二维码扫描库
```bash
npm install @yudiel/react-qr-scanner
```

## 完整安装命令

在 `frontend` 目录下运行：

```bash
cd frontend
npm install qrcode @yudiel/react-qr-scanner
npm install --save-dev @types/qrcode
```

## 依赖说明

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| `qrcode` | ^1.5.x | 生成二维码图片 |
| `@types/qrcode` | ^1.5.x | QRCode 的 TypeScript 类型定义 |
| `@yudiel/react-qr-scanner` | ^2.0.x | React 二维码扫描组件 |

## 功能对应

- **用户端（我的门票页面）**：使用 `qrcode` 生成门票二维码
- **组织者端（检票系统页面）**：使用 `@yudiel/react-qr-scanner` 扫描二维码

## 安装后

安装完成后，重启开发服务器：

```bash
npm run dev
```

## 可能的问题

### 问题 1：摄像头权限
如果扫描页面无法访问摄像头，请确保：
1. 浏览器已授予摄像头权限
2. 使用 HTTPS 或 localhost（HTTP 可能被浏览器限制）

### 问题 2：类型错误
如果出现 TypeScript 类型错误，确保已安装 `@types/qrcode`：
```bash
npm install --save-dev @types/qrcode
```

### 问题 3：构建错误
如果出现构建错误，尝试清除缓存：
```bash
rm -rf .next
npm run dev
```
